generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Schema will be populated in Step 3+
// This is a placeholder to allow `prisma generate` to work
